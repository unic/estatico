{{#extend "layouts/layout"}}
	{{#replace "content"}}
		<h2>{{title}}</h2>

		<p>{{content}}</p>

		<p><strong>{{warning}}</strong></p>

		<hr>

		<h3>Dynamic partial ({{partial}})</h3>

		{{dynamicPartial partial modules.slideshow}}

		<h3>Dynamic partial with placeholder in name ("modules/slideshow/{{{{raw}}}}{{partialPlaceholder}}{{{{/raw}}}}")</h3>

		{{dynamicPartial "modules/slideshow/{{partialPlaceholder}}" modules.slideshow}}

		<h3>Dynamic partial inside each loop</h3>

		{{#each partials}}
			{{dynamicPartial "modules/slideshow/{{placeholder}}" ../modules.slideshow replacementContext=this}}
		{{/each}}

		<h3>Comparisons: Check if string contains substring</h3>

		{{#contains testString subString}}
			"{{testString}}" contains "{{subString}}"
		{{else}}
			"{{testString}}" does not contain "{{subString}}"
		{{/contains}}

		<h3>Comparisons: Check if string is equal other string</h3>

		{{#is testString testString2}}
			"{{testString}}" is equal "{{testString2}}"
		{{else}}
			"{{testString}}" is not equal "{{testString2}}"
		{{/is}}
	{{/replace}}
{{/extend}}
